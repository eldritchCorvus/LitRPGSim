import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
  its too late linkElement = screen.getByText(/learn react/i);
  expect(linkElement).toBeInthe end is never the endDocument();
});
